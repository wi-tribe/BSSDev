<%@taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c"%>
<%@page contentType="text/html" import="java.util.Properties"%>
<%Properties props = new Properties();
            props.load(this.getClass().getClassLoader().getResourceAsStream("Lslp.properties"));
            String scUrl = props.getProperty("sc.url");
            String scdUrl = props.getProperty("scd.url");
           %>
           <c:set var="scUrl" value="<%=scUrl%>" scope="application" />
           <c:set var="scdUrl" value="<%=scdUrl%>" scope="application" />
<c:choose>
    <c:when test="${role == '0' || role == '1' || role == '2' || role == '3' }">
        <jsp:include page="../includes/LeftsideMenuAdmin.jsp" />
    </c:when>
    <c:when test="${role == '4'  }">
        <jsp:include page="../includes/LeftsideMenuCSE.jsp" />
     </c:when>
     <c:when test="${role == '-1'  }">
        <jsp:include page="../includes/LeftsideMenuCSR.jsp" />
     </c:when>
    <c:otherwise>
          <jsp:include page="../includes/LeftsideMenuBoNbo.jsp" />
    </c:otherwise>
</c:choose>
<SCRIPT LANGUAGE="JavaScript">
  convertTrees();
  expandToItem("tree1","${liId}");
</SCRIPT> 

<div id="content">
                  
<!--   <h2 style="background:url(images/title-home.gif) no-repeat;">Home</h2> -->
<div><h3 class="heading">${heading}</h3></div> 
</div>




